Call the `{action}` tool for this release.

- Release ID: {release_id}
- Jira issue: {jira_issue_key}
- Risk level: {risk_level}

Use only the tool that matches the action. Do not invent approval URLs, mail bodies, or Jira transitions — the tool performs that work.
