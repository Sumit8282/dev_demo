Re-map only these coverage **gaps** for Jira **{jira_issue_key}**. This is a single review. Do not request another pass.

## Gap rows after Python constraints
{gap_rows}

## Extracted QA test cases (authoritative list)
{test_cases}

Return one coverage-matrix row per gap AC. Use only these test cases. Judge by intent, not keyword overlap.
