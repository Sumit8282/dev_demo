Generate one stdlib-only pytest script per acceptance criterion.

## Jira issue
{jira_issue_key}

## PR head SHA
{head_sha}

The portal cloned this SHA. Use `assert_mentioned(...)` with strings copied from the excerpts. Python may replace `test_code` with a phrase search over the real checkout.

## Acceptance criteria
{acceptance_criteria}

## Repo files and excerpts
{repo_files}

## Existing tests / evidence (style only)
{existing_tests}

Return structured `tests`.
