Create the RM approval request for release **{release_id}**.

- Build ID: {build_id}
- Target environment: {environment}
- Jira issue: {jira_issue_key}

Call create_rm_approval_request. Do not invent approval URLs or notification content — the tool performs that work.
